Some test examples

Complile the code with make, the config.properties files are in the example directories. 

= BasicProtocol: A very simple protocol
= CounterProtocol: The basic protocol with a message counter. The message counter should not be a happy byte.  